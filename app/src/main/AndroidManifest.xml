<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectdam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Vista.Activity.ImagenProducto"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert">
        </activity>
        <activity android:name=".Vista.Activity.ListaProductos" />
        <activity android:name=".Vista.Activity.ListaCategorias" />
        <activity android:name=".Vista.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Vista.Activity.PerdidoContrasenya" /> <!-- MENU PRINCIPAL -->
        <!-- <activity android:name=".Menu.MenuPrincipal"/> -->
        <activity
            android:name=".Vista.Activity.Activity_Menu"
            android:screenOrientation="portrait" /> <!-- USERS -->
        <activity android:name=".Vista.Activity.MenuUser_add" />
        <activity android:name=".Vista.Activity.Activity_GestioUserModVer" />
        <activity android:name=".Vista.Activity.Activity_VerPedido" />
        <activity android:name=".Vista.Activity.Activity_ModPedido" />
        <activity android:name=".Vista.Activity.Activity_all_Products" />
    </application>

</manifest>